name: Deploy Frontend

on:
  push:
    branches:
      - main

env:
  IMAGE_NAME: react-app

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      #1. get the latest code
      - name: Checkout code
        uses: actions/checkout@v4

      #2. gate the build on lint + typecheck
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npx tsc -b

      #3. Login to Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      #4. Build and push image
      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      #5. Deploy to VPS
      # Requires secrets: VPS_HOST, VPS_USER, VPS_SSH_KEY (and optionally VPS_PORT,
      # VPS_APP_DIR). Skipped automatically until VPS_HOST is set.
      # - name: Deploy to VPS
      #   if: ${{ secrets.VPS_HOST != '' }}
      #   uses: appleboy/ssh-action@v1
      #   with:
      #     host: ${{ secrets.VPS_HOST }}
      #     username: ${{ secrets.VPS_USER }}
      #     key: ${{ secrets.VPS_SSH_KEY }}
      #     port: ${{ secrets.VPS_PORT || 22 }}
      #     envs: IMAGE_TAG,DOCKERHUB_USERNAME
      #     script: |
      #       set -euo pipefail
      #       cd "${{ secrets.VPS_APP_DIR || '~/react-app' }}"
      #       docker compose pull
      #       docker compose up -d --remove-orphans
      #       docker image prune -f
      #   env:
      #     IMAGE_TAG: ${{ github.sha }}
      #     DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
